# pillow-rem

This is a simple Python script that demonstrates the use of the Pillow library to remove the background from an image.

## Usage

1. Install the required packages:

```bash
pip install pillow numpy
```

2. Run the script:

```bash
python pillow_rem.py
```

## The queue agent read it and left his trace 🐾